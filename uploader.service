[Unit]
Description=Uploader
Requires=docker.service
After=docker.service

[Service]
Restart=always
ExecStart=/usr/bin/docker start -a uploader
ExecStop=/usr/bin/docker stop uploader

[Install]
WantedBy=default.target
